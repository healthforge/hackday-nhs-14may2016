graph

  .hover-value
    font-size: 16px;
    margin: 10px 0 0;
    font-weight: bold;

    .units
      font-size: 14px;
      font-weight: normal;

  .graph-label
    h2
      font-size: 20px;
      margin: 10px 0;

  .graph-container:nth-child(odd) {
    background-color: #f8f8f8;
  }

